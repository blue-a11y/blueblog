# BlueBlog 每日任务 - 2026-04-04

## 轮次 1（已完成）
- [x] 第1轮：读取 `PROGRESS.md` 与昨日任务，确认 Phase 4 已完成，切入 Phase 5
- [x] 第1轮：spawn `project-agent` 推进 Phase 5 的首个高价值任务
- [x] 第1轮：记录产出、更新任务状态和进度

## 轮次 2
- [x] 第2轮：检查第1轮结果，继续推进 Phase 5
- [x] 第2轮：更新任务状态和进度

## 轮次 3
- [x] 第3轮：检查第2轮结果，继续推进 Phase 5
- [x] 第3轮：更新任务状态和进度

## 轮次 4（收尾）
- [x] 第4轮：收尾工作
- [x] 第4轮：npm run build 验证
- [x] 第4轮：git add -A && git commit -m "chore: update" && git push origin main（补检查：当前工作树已 clean，无新增未提交改动）
- [x] 第4轮：截图并发送飞书总结
- [x] 第4轮：清除本节心跳项

## 项目进度
**当前阶段**: Phase 5 - SEO + RSS + 搜索 + 性能优化 + 部署
**状态**: 🟡 进行中（SEO / RSS / 搜索 / 性能优化 / 首次部署已落地；本轮已补主题颜色回归修复，当前剩自定义域名收尾）

### 启动判断（2026-04-04 04:18）
- [x] 已核对 `PROGRESS.md`：Phase 4 已于 04:00 完成验收并收尾
- [x] 已核对昨日任务：昨天的收尾任务已经完成，不再重复 Phase 4 验收
- [x] 今日应切入 Phase 5，而不是重复做 Phase 4

### 今日第1轮目标（优先级顺序）
1. **SEO 基线搭建**
   - 评估并优先落地 sitemap、robots、metadata 基线
   - 若已有部分实现，补齐缺口并统一站点信息
2. **RSS / 搜索 / 性能 的路线确认**
   - 在代码和文档中明确 Phase 5 的拆分顺序
   - 若时间足够，顺手落一个最小可用项
3. **强制同步文档**
   - 完成任何代码产出后，先更新 `PROGRESS.md`
   - 再回填本任务文档，写清本轮完成项 / 未完成项 / 下一轮建议

## 第1轮回填（2026-04-04 04:28）

### 本轮完成项
- 完成 **SEO 基线**：新增 `lib/site.ts` 统一站点 URL / title / description / keywords / OG 图地址等配置。
- 完成 **根 metadata 重构**：`app/layout.tsx` 已补齐 `metadataBase`、标题模板、canonical、robots、Open Graph、Twitter。
- 完成 **页面级 metadata 补齐**：`/blog`、`/blog/[slug]`、`/about`、`/projects`、`/playground`、`/showcase`、`/components-examples` 均已补上描述、canonical、分享卡片。
- 完成 **站点地图与爬虫规则**：新增 `app/sitemap.ts`、`app/robots.ts`，已覆盖静态路由与已发布博客文章。
- 完成 **分享图基线**：新增 `app/opengraph-image.tsx`、`app/twitter-image.tsx`，避免社交分享只显示裸标题。

### 验证结果
- `npm run lint` ✅
- `npm run build` ✅
- `npm run dev` 后实测访问以下路径均返回 200：`/`、`/blog`、`/blog/2026-03-30-shipping-clean-next-js-layouts`、`/about`、`/projects`、`/robots.txt`、`/sitemap.xml`
- 额外验证：`/opengraph-image`、`/twitter-image` 均返回 `image/png` ✅

### 下一轮建议
1. **RSS 生成**：优先做 `/feed.xml`，可直接复用 `lib/posts.ts` 的已发布文章数据，和 sitemap 一样属于高价值低风险项。
2. **全文搜索**：做静态 JSON 索引或轻量客户端索引，先把博客搜索最小可用版落地。
3. **性能优化**：等 RSS / 搜索到位后，再看 ISR、图片策略和 bundle 体积，别一上来瞎抠皮毛。

## 第5轮回填（2026-04-04 14:30）

### 本轮完成项
- 完成 **内容缓存与 ISR 收口**：`lib/posts.ts` 使用 `unstable_cache` 缓存文章读取与 frontmatter 解析，`lib/search.ts` 增加 1 小时缓存，`/blog`、`/blog/[slug]`、`/sitemap.xml` 补齐 `revalidate = 3600`。
- 完成 **客户端包袱减重**：`ThemeToggle`、首页 Hero CTA、博客列表搜索 / 筛选 / 卡片交互去掉 HeroUI 客户端组件，改为原生元素，避免一个博客页为了几个按钮背额外包。
- 完成 **构建体积对比**：`npm run build` 结果显示首页 `First Load JS` 从 `130 kB` 降到 `106 kB`，博客页从 `135 kB` 降到 `104 kB`，优化不是嘴上说说。
- 明确 **图片优化边界**：当前站内没有真实文章封面或内容图，本轮没有硬凑 `next/image` 改造；后续若补封面素材，再顺手接入响应式图片即可。

### 验证结果
- `npm run lint` ✅
- `npm run build` ✅
- 构建体积变化：
  - `/` → `130 kB` 降至 `106 kB`
  - `/blog` → `135 kB` 降至 `104 kB`
  - `/blog/[slug]` 保持 `106 kB`

### 下一步
1. 启动 `npm run dev` 做运行态回归，并补新的截图证据到 artifacts。
2. 检查 Vercel CLI / 项目链接 / 登录状态，能部署就直接推进首个部署。
3. 如果卡在 Vercel 授权、账号或域名权限，就立刻记录阻塞，别装作没事。

## 第7轮回填（2026-04-04 14:20）

### 本轮完成项
- 完成 **Vercel 登录校验**：`npx vercel whoami` 已返回 `blue-a11y`，说明当前机器上的 Vercel 凭据可用。
- 完成 **项目 link**：在 `app/` 下执行 `npx vercel link --yes`，项目已关联到 `blues-projects-90e3f68b/app`，并生成 `.vercel/` 本地配置。
- 确认 **项目连接状态**：当前 deploy 可以直接在 `app/` 下执行，不再卡在登录和项目绑定这两个低级坎上。

### 验证结果
- `npx vercel whoami` ✅ `blue-a11y`
- `npx vercel link --yes` ✅
- 关联结果：`blues-projects-90e3f68b/app`

### 下一步
1. 直接执行首个 production deploy，拿到线上 deployment URL。
2. 对线上 `/`、`/blog`、文章页、`/feed.xml`、`/search-index.json` 做实测。
3. 如果线上构建或运行报错，先修代码再重部署，别装死。

## 第8轮回填（2026-04-04 14:24）

### 本轮完成项
- 完成 **错误部署定位**：第一次从 `app/` 目录发起 deploy，结果整站线上全 `404`。原因不是代码烂，是把 Next.js 的 `app/` 子目录错当成项目根，Vercel 实际部署了错误的目录。
- 完成 **根目录重连**：在仓库根目录重新执行 `npx vercel link --yes`，Vercel 正确识别项目为 Next.js，并关联到 `blues-projects-90e3f68b/blueblog`。
- 完成 **首次 production deploy**：根目录执行 `npx vercel deploy --prod --yes`，生成 deployment URL：`https://blueblog-17nzgbht1-blues-projects-90e3f68b.vercel.app`。
- 完成 **线上验收**：公开 production alias `https://blueblog-theta.vercel.app` 已可访问，实测 `/`、`/blog`、`/blog/2026-03-30-shipping-clean-next-js-layouts`、`/feed.xml`、`/search-index.json` 全部返回 `200`。

### 验证结果
- `npx vercel inspect blueblog-17nzgbht1-blues-projects-90e3f68b.vercel.app` ✅ `status: Ready`
- 公开 alias：`https://blueblog-theta.vercel.app`
- 路由验证：
  - `/` → `200 text/html; charset=utf-8`
  - `/blog` → `200 text/html; charset=utf-8`
  - `/blog/2026-03-30-shipping-clean-next-js-layouts` → `200 text/html; charset=utf-8`
  - `/feed.xml` → `200 application/rss+xml; charset=utf-8`
  - `/search-index.json` → `200 application/json`
- 备注：deployment 直链因 Vercel 访问保护返回 `401`，但公开 alias 正常，不影响外部访问。

### 下一步
1. 处理 git 状态，把本轮性能优化 + 部署文档更新整理提交。
2. 若 remote 正常可推送，则 push 到 `origin/main`。
3. 自定义域名仍待用户明确域名与 DNS 权限，先不要自作聪明乱绑。

## 第6轮回填（2026-04-04 14:45）

### 本轮完成项
- 完成 **运行态回归**：启动 `npm run dev -- --port 3010`，实际请求 `/`、`/blog`、`/blog/2026-03-30-shipping-clean-next-js-layouts`、`/feed.xml`、`/search-index.json`，全部返回 `200`，内容类型正常。
- 完成 **证据归档**：新增 `.openclaw/artifacts/2026-04-04-round5/`，保存每个关键路径的 `headers.txt` 和 `body`，并使用 Playwright 生成 `home-desktop.png`、`blog-desktop.png`、`post-desktop.png`、`feed-desktop.png`。
- 完成 **Vercel 部署预检**：仓库内确认暂无 `.vercel/` 和 `vercel.json`；`npx vercel --version` 可用，但 `npx vercel whoami` 直接报 `No existing credentials found`。
- 明确 **部署阻塞**：当前机器没有已登录的 Vercel 凭据，无法继续 `vercel link` 或首次 `deploy`。这不是代码问题，是账号授权问题。

### 验证结果
- `npm run dev -- --port 3010` ✅
- 关键路径访问：
  - `/` → `200 text/html; charset=utf-8`
  - `/blog` → `200 text/html; charset=utf-8`
  - `/blog/2026-03-30-shipping-clean-next-js-layouts` → `200 text/html; charset=utf-8`
  - `/feed.xml` → `200 application/rss+xml; charset=utf-8`
  - `/search-index.json` → `200 application/json`
- `npx vercel --version` ✅
- `npx vercel whoami` ❌ `No existing credentials found`

### 下一步
1. 让用户先在本机完成 `npx vercel login`（或提供可用 token）。
2. 登录后执行 `npx vercel link` 关联项目，再做首个预览部署。
3. 部署成功后再继续自定义域名 / DNS 接入，不要在没登录的状态下空想。

## 第9轮回填（2026-04-04 14:55）

### 本轮完成项
- 启动 `npm run dev -- --port 3020`，实际查看首页、`/blog`、文章页在明 / 暗主题下的运行态，并用 Playwright 截图对比；这次不是靠猜。
- 定位到颜色问题的根因是 **主题 token 对比度不足**：亮色模式里次级文字、边框、卡片层级太淡；暗色模式里卡片、正文容器、代码块与背景距离太近。
- 修复 `app/globals.css`：仅增强明暗两套主题下的 `surface`、`card`、`muted`、`muted-foreground`、`border`、`shadow`，保持原有克制风格，不重做视觉。
- 修复 `app/blog/[slug]/page.tsx`：文章正文外层容器改为 `bg-surface/96`，并略增强边框 / 阴影，让正文卡与内部代码块、页面背景分层更清楚。

### 验证结果
- 运行态复查页面：`/`、`/blog`、`/blog/2026-03-30-shipping-clean-next-js-layouts`
- 主题复查范围：以上页面均检查 light / dark 两套主题 ✅
- `npm run lint` ✅
- `npm run build` ✅

### 下一步
1. 提交本轮主题修复。
2. remote 正常就直接 push。
3. 线上如需同样复查，再针对公开 alias 做一轮视觉 spot check。

## 备注
- 今日工作目标已从“启动 Phase 5”推进到“完成性能优化、首次部署与主题颜色回归修复”。
- 每轮结束前都要检查：代码、`PROGRESS.md`、任务文档三者是否一致。
- 部署遇阻时必须把阻塞点写清楚，别写空气总结。

## 第3轮回填（2026-04-04 05:30）

### 本轮完成项
- 完成 **搜索索引基线**：新增 `app/search-index.json/route.ts` 与 `lib/search.ts`，输出已发布文章的静态 JSON 索引（标题、描述、摘要、标签、生成时间、总数）。
- 完成 **搜索逻辑收口**：新增 `lib/blog-search.ts` 与 `BlogSearchEntry` 类型，统一搜索文本拼接和匹配逻辑，避免组件里乱写字符串拼盘。
- 完成 **博客页搜索升级**：`app/blog/page.tsx` 现在会同时加载文章列表、标签和搜索索引；`components/post-list.tsx` 已支持标题 / 摘要 / 标签搜索，并补齐结果计数、查询态文案和更完整的空状态。
- 完成 **体验克制化处理**：保留现有页面气质，只加轻量搜索说明和状态反馈，没有引入复杂面板或大重构。

### 验证结果
- `npm run lint` ✅
- `npm run build` ✅
- `npm run dev -- --port 3003` 后实测 `http://localhost:3003/blog` 返回 `200`，页面 HTML 已包含搜索输入提示、结果说明文案与搜索模块 ✅
- `http://localhost:3003/search-index.json` 返回 `200`，JSON 中包含 `generatedAt`、`total: 2` 与两篇已发布文章的索引数据 ✅
- 额外脚本验证搜索逻辑：关键词 `next` 仅命中 `Shipping Clean Next.js Layouts`，关键词 `ux` 命中 `Designing Silent Interfaces`，关键词 `definitely-no-match` 返回空数组，对应页面空状态文案已存在 ✅

### 下一轮建议
1. **性能优化**：优先看博客和首页的缓存 / ISR 策略，确认哪些页面可静态化、哪些数据适合 revalidate。
2. **图片与体积**：检查站内图片策略、OG 生成成本、首屏 JS 体积，必要时做 bundle 分析，别让一个博客站背着健身房器械跑步。
3. **部署准备**：在性能项收口后推进 Vercel 配置和域名接入，把上线链路补齐。

## 第4轮回填（2026-04-04 05:55）

### 本轮完成项
- 完成 **最终构建回归**：再次执行 `npm run build`，确认当前 Phase 5 基线在收尾阶段仍可稳定产出。
- 完成 **运行态路由验证**：启动 `npm run dev -- --port 3004`，实测 `/`、`/blog`、`/blog/2026-03-30-shipping-clean-next-js-layouts`、`/feed.xml`、`/search-index.json`、`/robots.txt`、`/sitemap.xml` 均返回 `200`，内容类型正常。
- 完成 **截图归档**：新增 `.openclaw/artifacts/2026-04-04-round4/`，保存 `blog-search-desktop.png`、`blog-search-mobile.png`、`feed-desktop.png`，覆盖博客搜索结果与 RSS feed 页面。
- 完成 **文档同步**：已先更新 `PROGRESS.md`，明确 Phase 5 当前状态为“SEO / RSS / 搜索已落地，进入性能与部署准备”，再回填本任务文档。

### 验证结果
- `npm run build` ✅
- `npm run dev -- --port 3004` ✅
- 路由与内容类型验证：
  - `/` → `200 text/html`
  - `/blog` → `200 text/html`
  - `/blog/2026-03-30-shipping-clean-next-js-layouts` → `200 text/html`
  - `/feed.xml` → `200 application/rss+xml`
  - `/search-index.json` → `200 application/json`
  - `/robots.txt` → `200 text/plain`
  - `/sitemap.xml` → `200 application/xml`

### 后续建议
1. **性能优化**：优先看可静态化页面、`revalidate` 策略、首屏 JS 体积和 OG 生成成本，别让一个博客站继续背着无意义的包袱。
2. **部署准备**：补 Vercel 项目配置、环境变量核对和自定义域名接入，把上线链路走通。
3. **发布前回归**：正式上线前再跑一次 build + 关键路由 + feed / sitemap / 搜索检查，确保发现链路和分享链路都没再被改炸。

## 第2轮回填（2026-04-04 04:52）

### 本轮完成项
- 完成 **RSS 订阅输出**：新增 `app/feed.xml/route.ts`，基于已发布博客文章动态生成 `/feed.xml`。
- 完成 **Feed 基础信息收口**：`lib/site.ts` 新增 `feedPath` 与 `getFeedUrl()`，统一 feed 入口，避免路径散落硬编码。
- 完成 **订阅发现入口**：`app/layout.tsx` 与 `app/blog/page.tsx` 的 metadata alternates 已声明 `application/rss+xml`。
- Feed 输出已包含站点 channel 信息、文章 permalink、发布日期、标签分类与缓存策略。

### 验证结果
- `npm run lint` ✅
- `npm run build` ✅
- `npm run dev` 后实测访问 `http://localhost:3002/feed.xml`：返回 `200` ✅
- 头部验证：`content-type: application/rss+xml; charset=utf-8` ✅
- 内容验证：XML 中已包含 `channel`、`atom:link`、已发布文章的 `<item>`、文章链接、`pubDate`、`category` ✅
- 回归验证：`/blog` 返回 `200` ✅

### 下一轮建议
1. **全文搜索最小可用版**：优先做博客文章静态 JSON 索引 + 客户端标题/摘要/标签搜索，价值高、风险低。
2. **搜索体验顺手补齐**：搜索框支持空状态、结果计数、高亮关键词即可，别一上来就造复杂搜索引擎。
3. **性能优化放后面**：等搜索落地后，再看 ISR、图片策略和 bundle 分析，先把核心发现链路补齐。

## 第10轮回填（2026-04-04 15:05）

### 本轮完成项
- 核查 `git status` 与 `git diff components/home-hero.tsx`，确认首页主 CTA 的修正已存在：`Read the blog` 按钮改为指向 `/blog` 的主链接样式，视觉为浅色强调底 + 细边框 + 圆角 pill，不再是之前那套旧 HeroUI 按钮。
- 执行 `npm run build` ✅，确认生产构建通过；随后启动 `npm run dev -- --port 3030`，实际打开首页并留存截图 `.openclaw/artifacts/2026-04-04-cta-deploy/home-cta-local.png`，本地 UI 已对齐预期。
- 已同步更新 `PROGRESS.md` 与本任务文档，补齐这次“代码已改但线上未部署”的收尾记录。
- 已完成提交、推送与 production 重新部署；部署操作明确在 **项目根目录** 进行，没有再犯把 `app/` 子目录错误 link 的蠢病。
- 已完成线上验证：`https://blueblog-theta.vercel.app/` 返回 `200`，首页可访问，主 CTA 新样式已上线。

### 验证结果
- `npm run build` ✅
- 本地运行态首页截图 ✅ `.openclaw/artifacts/2026-04-04-cta-deploy/home-cta-local.png`
- 线上首页：`https://blueblog-theta.vercel.app/` ✅

### 后续建议
1. 若还要继续改首页视觉，先改再 deploy，别再出现“文件改了但线上还是旧的”这种浪费时间的低级回环。
2. 自定义域名仍是 Phase 5 仅剩的大项，需要用户明确域名和 DNS 权限后再推进。

## 第11轮回填（2026-04-04 15:18）

### 本轮完成项
- 按 **Vercel Analytics 官方 Quickstart** 完成 Next.js 接入：安装 `@vercel/analytics`，并在根布局 `app/layout.tsx` 中引入 `Analytics` 组件，放在 `</body>` 前。
- 保持改动最小：除 `package.json` / `package-lock.json` / `app/layout.tsx` 外未动其他业务文件，也没有擅自加 Speed Insights 之类的可选玩意。
- 已完成文档同步：`PROGRESS.md` 与本任务文档均已回填本轮接入记录。

### 验证结果
- `npm run lint` ✅
- `npm run build` ✅
- 说明：本轮仅为根布局注入官方 Analytics 组件，构建与类型检查通过，未发现需要额外启动 dev server 才能定位的问题。

### 下一步
1. 提交并 push 本轮改动。
2. 在仓库根目录执行 production deploy，让 Analytics 代码真实上线。
3. 若项目面板尚未启用 Web Analytics，则提醒用户去 Vercel 面板打开。
