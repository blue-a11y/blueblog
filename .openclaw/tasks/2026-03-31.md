# BlueBlog 每日开发任务 - 2026-03-31

**日期**：2026年3月31日（星期二）  
**阶段**：Phase 2 - 博客系统开发  
**时间窗口**：04:00-06:00（共4轮）

### 🐛 待修复 Bug（Blue 反馈，2026-03-31 09:16）
- [ ] **文章详情页 404**：博客文章列表能展示，但点进去文章详情页返回 404
  - 可能原因：`generateStaticParams` 未正确生成 slug、路由文件位置错误、或 MDX 文件未被正确解析
  - 修复方向：检查 `app/blog/[slug]/page.tsx` 路由、slug 生成逻辑、MDX 文件内容目录
- [ ] **项目定位修正**：BlueBlog 是纯网页项目（部署 Vercel），**不是桌面应用**，截图时应只用浏览器移动端/桌面视口，不要用桌面壳截图

---

## 📋 今日总任务
**Phase 2：博客系统（MDX + 列表 + 详情 + 代码高亮）**
**目标**：完成博客系统核心功能，确保文章能正常创建、展示和访问

---

## 🎯 第一轮任务（04:00 - 已完成）
- [x] 创建任务文档并写入今日任务清单
- [x] Spawn project-agent 执行第一轮开发任务

### 第一轮启动参数：
- 读取 HeroUI v3 SKILL.md 获取组件用法
- 基于 PROGRESS.md 的 Phase 2 任务清单
- 开发重点：MDX 内容系统搭建（frontmatter + 文件解析）
- 使用 sessions_spawn(agentId: "project-agent", mode: "run")

---

## 🔄 待执行任务

### 🎯 第二轮任务（04:30）
- [ ] 读取 tasks/2026-03-31.md 检查第一轮结果
- [ ] Spawn project-agent 继续开发（MDX 内容系统验证）
- [ ] 重点：博客列表页开发（分类筛选 + 搜索 + 卡片网格）

### 🎯 第三轮任务（05:00）
- [ ] 读取 tasks/2026-03-31.md 检查第二轮结果  
- [ ] Spawn project-agent 继续开发（文章详情页 + Shiki 代码高亮）
- [ ] 重点：MDX 渲染 + 代码块主题与 Shiki 高亮风格统一

### 🎯 第四轮任务（05:30）
- [ ] 读取 tasks/2026-03-31.md 检查第三轮结果
- [ ] Spawn project-agent 完成收尾工作
- [ ] 执行 npm run build 验证构建
- [ ] 生成项目截图（桌面端 + 移动端）
- [ ] 发送飞书总结通知给 ou_709ef50549f56cf10e2f48368747f399
- [ ] 清除 HEARTBEAT.md 中的 BlueBlog 轮次项

---

## 📋 第一轮具体任务（待 project-agent 执行）
### Phase 2.1：MDX 内容系统搭建
- [ ] 搭建 MDX 内容系统架构
- [ ] 创建文章数据类型定义（TypeScript interface）
- [ ] 实现 frontmatter 解析逻辑
- [ ] 文件系统遍历与文章列表生成
- [ ] 文章 slug 生成规则实现
- [ ] 基础文章列表页面框架

### 技术要点：
- 使用 `gray-matter` 解析 frontmatter
- 文章存储位置：`contents/posts/`  
- Slug 格式：`YYYY-MM-DD-title-slug`
- 支持发布状态控制 `published: boolean`
- 支持分类标签 `tags: string[]`
- 支持文章时长估算 `readingTime: number`

---

## 🔗 相关文件路径
- 项目根目录：`/Users/Blue/.openclaw/workspace/projects/blueblog/`
- 源码目录：`/Users/Blue/.openclaw/workspace/projects/blueblog/src/`
- 内容目录：`/Users/Blue/.openclaw/workspace/projects/blueblog/contents/posts/`
- 组件目录：`/Users/Blue/.openclaw/workspace/projects/blueblog/src/components/`

---

## 📝 工作记录
（各轮任务完成后填写）

### 第一轮 - 04:00
**任务内容**：MDX 内容系统搭建（frontmatter + 文件解析）
**预期产出**：文章数据类型定义、frontmatter 解析、文件系统遍历逻辑
**实际产出**：
- 已新增 `types/post.ts`，定义 `PostFrontmatter`、`Post`、`PostSummary` 数据结构，覆盖 `published`、`tags`、`readingTime` 等核心字段
- 已新增 `lib/posts.ts`，完成 `gray-matter` frontmatter 解析、日期规范化、slug 生成、阅读时长估算、摘要提取、文章遍历与排序逻辑
- 已建立 `contents/posts/` 内容目录，并补充 3 篇示例文章（其中 2 篇已发布、1 篇草稿）用于验证发布状态过滤
- 已新增 `app/blog/page.tsx` + `components/post-list.tsx`，完成基础博客列表页框架，展示文章卡片、标签、日期与阅读时长
- 已补充首页跳转到 `/blog` 的入口，并把本轮新增/修改的 UI 文案统一整理为英文
- 已安装依赖：`gray-matter`
- 已执行 `npm run build`，构建通过 ✅
**问题记录**：
- `gray-matter` 会将 YAML 日期解析为 `Date` 对象，已在 `lib/posts.ts` 中增加日期归一化处理，否则静态构建会在 `/blog` 预渲染时报错
- HeroUI v3 的 `Button` 类型不接受 `as={Link}` 这种写法，已改为 `onPress` 跳转，避免类型报错
**下一步计划**：
- 下一轮继续做博客列表页增强：分类筛选、搜索、卡片网格细化
- 后续补上 `/blog/[slug]` 详情页与 MDX 渲染链路

### 第二轮 - 04:30
**任务内容**：博客列表页增强（筛选 + 搜索 + 网格 + 空状态 + 加载更多）
**预期产出**：可交互的博客列表体验，支持客户端过滤与更友好的列表浏览
**实际产出**：
- 已重构 `components/post-list.tsx` 为客户端交互列表，新增 `activeTag`、`query`、`visibleCount` 三组状态，支持标签筛选、标题/摘要搜索与分批展示
- 已加入标签过滤区与卡片内标签快捷筛选，点击标签即可切换当前分类，另补充 `Clear filters` / `Reset view` 交互
- 已新增搜索输入框，按标题、描述、摘要进行客户端模糊匹配，并实时反馈结果数量与当前查询状态
- 已优化文章卡片网格为响应式布局：移动端单列、平板双列、超宽屏三列，整体信息密度更合理
- 已加入双层空状态：无发布文章时提示发布首篇文章；筛选无结果时提示重置视图
- 已新增 `Load more` 按钮，默认先展示 6 篇，后续按 6 篇递增，避免列表过长时首屏过重
- 已执行两次 `npm run build` 进行中途验证与最终验证，构建通过 ✅
**问题记录**：
- HeroUI v3 事件规范要求优先使用 `onPress`，卡片内标签交互已统一改成 `Button` 组件，避免混用原生 `onClick`
- 当前搜索为纯客户端过滤，后续如果文章规模显著增大，再考虑迁移到 URL query 或服务端搜索索引
**下一步计划**：
- 下一轮进入 `/blog/[slug]` 文章详情页与 MDX 渲染链路
- 补齐代码高亮、文章导航和详情页 SEO 元数据

---


### 第三轮 - 05:00
**任务内容**：文章详情页 + MDX 渲染 + Shiki 代码高亮
**预期产出**：`/blog/[slug]` 详情页、静态预渲染、SEO 元数据、代码块高亮与上下篇导航
**实际产出**：
- 已新增 `app/blog/[slug]/page.tsx`，完成动态详情页路由、`generateStaticParams` 静态预渲染、`generateMetadata` SEO 元数据生成，以及 slug 不存在时的 `notFound()` 处理
- 已新增 `lib/mdx.tsx`，基于 `next-mdx-remote/rsc` + `remark-gfm` + `rehype-pretty-code` 打通 MDX 服务端渲染链路，支持 Markdown/MDX 内容直接渲染为详情页正文
- 已新增 `components/mdx-components.tsx`，统一接管段落、标题、列表、引用、链接、行内代码等排版，并提供 `Callout` 自定义 MDX 组件用于验证组件渲染能力
- 已在 `app/globals.css` 中补充 `prose-shell` 与代码块样式，接入 Shiki 双主题（GitHub Light / GitHub Dark），支持语言标签、行号、高亮行/字符展示
- 已扩展 `lib/posts.ts`，支持按发布状态查询单篇文章，并新增上一篇/下一篇文章导航数据获取逻辑
- 已更新示例文章 `2026-03-30-shipping-clean-nextjs-layouts.mdx`，加入 `Callout` 组件和 `tsx` 代码块，验证 MDX 组件与代码高亮链路
- 已执行两次 `npm run build`：首次发现 HeroUI 客户端依赖不能直接放在服务端详情页中，已改为服务端安全的原生结构；修正后再次构建通过 ✅
**问题记录**：
- HeroUI v3 当前入口会触发 `client-only` 依赖，不能直接在服务端 `app/blog/[slug]/page.tsx` 中导入；详情页已改为原生 `Link` + Tailwind 布局，避免 SSR 构建报错
- 代码高亮采用 `rehype-pretty-code` 驱动 Shiki，样式层需要额外处理 `data-line` / `highlighted` 等属性，否则只会有 token 颜色，没有完整阅读体验
**下一步计划**：
- 下一轮做收尾：补最终验收、截图与整体交互检查
- 如需进一步增强，可继续补目录导航、分享入口或封面图展示

### 第四轮 - 05:30
**任务内容**：收尾验证 + 截图 + 飞书总结
**实际产出**：
- ✅ npm run build 通过（所有页面静态预渲染成功）
- ✅ 截图完成（博客列表桌面端/移动端、文章详情页、首页）
- ✅ 飞书总结通知已发送

**Phase 2 总结**：
4 轮工作全部完成，博客系统核心功能已就绪：
- MDX 内容系统（frontmatter 解析 + 文件遍历 + slug 生成）
- 博客列表页（标签筛选 + 搜索 + 响应式网格 + 分批加载）
- 文章详情页（动态路由 + SEO 元数据 + 上下篇导航）
- MDX 渲染链路（next-mdx-remote + remark-gfm + rehype-pretty-code）
- Shiki 代码高亮（双主题 GitHub Light/Dark + 语言标签 + 行号）

---

## ⚠️ 开发规范
- 所有 UI 文案必须为简洁英文，无开发注释
- 代码注释可用中文，但标签内文本必须为英文
- 严格遵循 HeroUI v3 使用规范（`onPress` 事件，compound pattern）
- 每次提交前运行 `npm run build` 验证